# Lightweight requirements for Azure App Service deployment
# Excludes heavy ML deps (darts, sklearn, xgboost, lightgbm) and notebooks
# Trading integration endpoints work fully without ML deps via lazy imports

# Core
python-dotenv>=1.0.0
pandas>=2.0.0
numpy>=1.24.0

# Database (Azure SQL)
sqlalchemy>=2.0.0
pyodbc>=5.0.0

# API
fastapi>=0.100.0
uvicorn>=0.23.0
gunicorn>=21.2.0

# Analytics (used in demand.py, seasonality.py)
scipy>=1.11.0
statsmodels>=0.14.0

# Data collection
requests>=2.31.0
requests-cache>=1.1.0
beautifulsoup4>=4.12.0
pyluach>=2.2.0
geopy>=2.4.0
pyarrow>=14.0.0
